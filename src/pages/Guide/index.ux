<template>
  <div class="wrapper">
    <swiper
      class="guide-swiper"
      index="0"
      indicator="true">
      <div class="guide-slide">
        <div class="slide-content">
          <image
            class="guide-icon"
            src="./../../assets/images/logo.png"></image>
          <text class="guide-title">{{ $t('guide.welcome') }}</text>
            <text class="guide-desc">{{ $t('guide.welcome_desc') }}</text>
        </div>
      </div>

      <div class="guide-slide">
        <div class="slide-content">
          <div class="icon-box">
            <text class="icon-text">÷×</text>
          </div>
          <text class="guide-title">{{ $t('guide.basic') }}</text>
          <text class="guide-desc">{{ $t('guide.basic_desc') }}</text>
        </div>
      </div>

      <div class="guide-slide">
        <div class="slide-content">
          <div class="icon-box">
            <text class="icon-text">2nd</text>
          </div>
          <text class="guide-title">{{ $t('guide.scientific') }}</text>
          <text class="guide-desc">{{ $t('guide.scientific_desc') }}</text>
        </div>
      </div>

      <div class="guide-slide">
        <div class="slide-content">
          <div class="icon-box">
            <text class="icon-text">📜</text>
          </div>
          <text class="guide-title">{{ $t('guide.history') }}</text>
          <text class="guide-desc">{{ $t('guide.history_desc') }}</text>
        </div>
      </div>

      <div class="guide-slide">
        <div class="slide-content">
          <div class="icon-box">
            <text class="icon-text">←→</text>
          </div>
          <text class="guide-title">{{ $t('guide.swipe') }}</text>
          <text class="guide-desc">{{ $t('guide.swipe_desc') }}</text>
        </div>
      </div>

      <div class="guide-slide">
        <div class="slide-content">
          <text class="guide-title">{{ $t('guide.start') }}</text>
          <text class="guide-desc">{{ $t('guide.start_desc') }}</text>
        </div>
        <div class="start-btn-wrapper" style="margin-top: 36px">
          <div class="start-btn" onclick="startUsing">
            <text class="start-text">{{ $t('guide.start') }}</text>
          </div>
        </div>
      </div>
    </swiper>
  </div>
</template>

<script>
  export default {
    data: {},

    startUsing() {
      global.storage.set({
        key: 'hasSeenGuide',
        value: 'true',
      })
      global.router.replace({
        uri: '/pages/Home',
      })
    },
  }
</script>

<style lang="scss">
  @import './../../assets/styles/style.scss';

  .wrapper {
    width: 100%;
    height: 100%;
    @include flex-box-mixins(column, flex-start, center);
  }

  .guide-swiper {
    width: 100%;
    height: 100%;
  }

  .guide-slide {
    width: 100%;
    height: 100%;
    @include flex-box-mixins(column, center, center);
  }

  .slide-content {
    @include flex-box-mixins(column, center, center);
  }

  .start-btn-wrapper {
    width: 100%;
    @include flex-box-mixins(row, center, center);
    margin-top: 60px;
  }

  .guide-icon {
    width: 150px;
    height: 150px;
    margin-bottom: 40px;
  }

  .icon-box {
    width: 140px;
    height: 140px;
    border-radius: 28px;
    background-color: $card-bg;
    @include flex-box-mixins(row, center, center);
    margin-bottom: 48px;
  }

  .icon-text {
    font-size: 48px;
    font-weight: bold;
    color: $white;
  }

  .guide-title {
    font-size: 56px;
    font-weight: 600;
    color: $white;
    margin-bottom: 20px;
  }

  .guide-desc {
    font-size: 36px;
    color: $gray;
  }

  .start-btn {
    width: 240px;
    padding: 22px 0;
    background-color: $brand;
    border-radius: 48px;
    @include flex-box-mixins(row, center, center);

    &:active {
      opacity: 0.4;
    }
  }

  .start-text {
    font-size: 38px;
    font-weight: 600;
    color: $white;
    text-align: center;
  }
</style>
